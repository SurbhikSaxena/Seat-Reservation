To build and pkg(as jar) the application
 	mvn clean package
 It will automatically run the Test class.
 and generate the jar file inside the target folder.
 
 To run jar file, go to the path where the jar file is present and run the command
  java -jar Ticket_Service-0.0.1-SNAPSHOT.jar
 The application is started.
 
